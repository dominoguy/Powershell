# Turn verbose logging on.
log verbose %3

#Suppress GUI confirmation windows
option confirm:yes-to-all

#Filter Rules: newer files out by timestamp - check time format of machine in regional and language setttings
filter -*.bcss
filter cutoff:>%4

# Load current files against snapshot from the last patch.

load %1 %2

# Expand to affect subfolders and files.

expand all

# Select updated files.

select left.diff.files left.orphan.files

# delete selected files
delete left